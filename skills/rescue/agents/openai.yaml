interface:
  display_name: "Claude Code Rescue"
  short_description: "Args: --background, --wait, --resume, --resume-last, --fresh, --write, --model <model>, --effort <auto|low|medium|high|xhigh|max>, --prompt-file <path>, [task text]. Delegate substantial work through the built-in rescue subagent path."
